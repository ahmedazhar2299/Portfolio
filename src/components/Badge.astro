<div class="badge-shell inline-flex items-center">
  <span class="badge-inner">
    <slot />
  </span>
</div>

<style>
  .badge-shell {
    border-radius: 999px;
    padding: 1px;
    background: linear-gradient(120deg, rgba(56, 189, 248, 0.85), rgba(52, 211, 153, 0.7), rgba(14, 165, 233, 0.85));
    box-shadow: 0 8px 24px rgba(14, 165, 233, 0.16);
  }

  .badge-inner {
    border-radius: 999px;
    background: color-mix(in srgb, var(--surface) 96%, transparent);
    color: var(--text-main);
    font-size: 0.84rem;
    line-height: 1;
    padding: 0.5rem 0.85rem;
  }
</style>
