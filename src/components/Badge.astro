<div class="badge-shell inline-flex items-center">
  <span class="badge-inner">
    <slot />
  </span>
</div>

<style>
  .badge-shell {
    border-radius: 999px;
    padding: 1px;
    background: linear-gradient(130deg, rgba(17, 24, 39, 0.32), rgba(82, 82, 91, 0.22));
    box-shadow: 0 10px 24px rgba(15, 23, 42, 0.08);
  }

  .dark .badge-shell {
    background: linear-gradient(130deg, rgba(250, 250, 250, 0.25), rgba(113, 113, 122, 0.2));
  }

  .badge-inner {
    border-radius: 999px;
    background: color-mix(in srgb, var(--surface-strong) 90%, transparent);
    border: 1px solid var(--surface-border);
    color: var(--text-main);
    font-size: 0.83rem;
    line-height: 1;
    padding: 0.5rem 0.85rem;
  }
</style>
